<div class="flex items-center gap-3">
  <span class="text-sm text-white/80">
    <%= current_user.first_name %> <%= current_user.last_name %>
  </span>
  <span class="px-2 py-0.5 rounded-full text-xs bg-white/10 text-white/80 uppercase tracking-wide">
    <%= current_user.role %>
  </span>

  <%= link_to "Create new campaign", admin_campaign_wizard_path,
              class: "px-3 py-1.5 rounded-lg bg-brand-green/20 hover:bg-brand-green/30" %>
</div>