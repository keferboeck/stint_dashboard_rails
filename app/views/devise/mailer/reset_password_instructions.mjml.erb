<%= render layout: 'layouts/mailer' do %>
  <mj-text font-size="18px" font-weight="600">Reset your password</mj-text>
  <mj-text>Hello <%= @resource.first_name.presence || 'there' %>,</mj-text>
  <mj-text>Click the button below to set a new password for your account.</mj-text>
  <mj-button href="<%= edit_password_url(@resource, reset_password_token: @token) %>">
    Set new password
  </mj-button>
  <mj-text class="muted">If you didn’t request this, you can ignore this email.</mj-text>
<% end %>