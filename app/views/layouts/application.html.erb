<!DOCTYPE html>
<html>
<head>
  <title>Stint Dashboard</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />

  <meta name="csrf-token" content="<%= form_authenticity_token %>">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

  <link rel="icon" type="image/svg+xml" href="<%= asset_path('favicon.svg') %>">

</head>

<body data-controller="campaign-wizard"
      class="min-h-screen bg-[#0a0a0a] text-brand-white font-sans">

<% if user_signed_in? %>
  <div class="min-h-screen flex">
    <aside class="w-64 bg-brand-black/90 border-r border-white/10 hidden md:flex md:flex-col">
      <%= render "shared/sidebar" %>
    </aside>

    <div class="flex-1 flex flex-col min-w-0">
      <header class="sticky top-0 z-10 backdrop-blur bg-black/40 border-b border-white/10">
        <div class="px-4 md:px-6 h-14 flex items-center justify-between">
          <div class="text-sm text-white/70">
            <%= render "shared/flash" %>
          </div>

          <%= render "shared/header_actions" %>
        </div>
      </header>

      <main class="p-4 md:p-8">
        <%= yield %>
      </main>
    </div>
  </div>
<% else %>
  <div class="min-h-screen flex items-center justify-center px-4 relative overflow-hidden">
    <div class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_20%_10%,rgba(164,158,242,.15),transparent_40%),radial-gradient(circle_at_80%_20%,rgba(22,233,144,.12),transparent_35%)]"></div>
    <%= render "shared/flash" %>
    <%= yield %>
  </div>
<% end %>

</body>
</html>