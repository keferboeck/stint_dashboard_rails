<!-- BEGIN app/views/admin_notice_mailer/purge_future.html.erb -->
<table role="presentation" width="100%" style="border-collapse:collapse;">
  <tr>
    <td style="font-size:16px; line-height:1.6; color:rgba(255,255,255,0.85); padding-bottom:16px;">
      <strong><%= @counts[:campaigns] %></strong> future campaign(s) and
      <strong><%= @counts[:emails] %></strong> pending email(s) were deleted by
      <strong><%= @actor %></strong> at <strong><%= @human_time %></strong>.
    </td>
  </tr>

  <tr>
    <td style="font-size:16px; line-height:1.6; color:#ffffff; padding:8px 0;">
      What does this mean?
    </td>
  </tr>
  <tr>
    <td style="font-size:14px; line-height:1.6; color:rgba(255,255,255,0.80); padding-bottom:10px;">
      All campaigns scheduled in the future have been removed, along with any unsent emails attached to them.
      They will not be sent unless recreated and scheduled again.
    </td>
  </tr>

  <tr>
    <td style="font-size:16px; line-height:1.6; color:#ffffff; padding:16px 0 6px;">
      Audit details
    </td>
  </tr>
  <tr>
    <td style="font-size:14px; line-height:1.6; color:rgba(255,255,255,0.75);">
      Deleted campaigns: <strong><%= @counts[:campaigns] %></strong><br>
      Deleted pending emails: <strong><%= @counts[:emails] %></strong><br>
      Actor: <strong><%= @actor %></strong><br>
      Time: <strong><%= @human_time %></strong>
    </td>
  </tr>
</table>
<!-- END app/views/admin_notice_mailer/purge_future.html.erb -->